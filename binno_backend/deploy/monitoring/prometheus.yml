global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: binno-api
    dns_sd_configs:
      - names: ["binno"]
        type: A
        port: 8080
  - job_name: binno-dispatcher
    static_configs:
      - targets: ["dispatcher:9090"]
