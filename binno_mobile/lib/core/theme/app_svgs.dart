/// BINNO logotip aktivlari (SVG).
abstract class AppSvgs {
  static const _base = 'assets/logo';

  /// Navy "binno" with orange "nn", for white backgrounds.
  static const binnoNavy = '$_base/binno-navy.svg';

  /// White "binno" with orange "nn", for navy backgrounds.
  static const binnoWhite = '$_base/binno-white.svg';

  /// Monoxrom navy (aksentsiz).
  static const binnoMonoNavy = '$_base/binno-mono-navy.svg';

  /// Monoxrom oq (aksentsiz).
  static const binnoMonoWhite = '$_base/binno-mono-white.svg';

  /// The square mark (app icon / favicon).
  static const binnoMark = '$_base/binno-mark.svg';
}
