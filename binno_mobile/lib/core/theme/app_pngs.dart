/// BINNO raster assets (a fallback where SVG does not work).
abstract class AppPngs {
  static const _base = 'assets/logo';

  static const binnoNavy = '$_base/binno-navy.png';
  static const binnoWhite = '$_base/binno-white.png';
}
